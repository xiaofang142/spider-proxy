# Spider Proxy v3.2 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 v3.2 版本的三个核心功能：WebSocket 压缩扩展、WebSocket 帧详情显示、脚本调试功能

**Architecture:** 分三个阶段顺序实施，每个阶段独立可测试。阶段 1 添加压缩依赖和逻辑，阶段 2 扩展帧数据结构，阶段 3 增强脚本引擎日志能力。

**Tech Stack:** Dart, Flutter, archive 包 (压缩), dart_eval (脚本引擎)

---

## 阶段 1：WebSocket 压缩扩展支持

### Task 1.1: 添加 archive 依赖

**Files:**
- Modify: `code/pubspec.yaml`
- Test: `code/test/websocket/websocket_compression_test.dart` (新建)

- [ ] **Step 1: 修改 pubspec.yaml 添加 archive 依赖**

在 `code/pubspec.yaml` 的 `dependencies` 部分添加：

```yaml
# File Compression (v3.2)
archive: ^3.4.10
```

- [ ] **Step 2: 运行 flutter pub get**

```bash
cd code
flutter pub get
```

预期：成功安装 archive 包

- [ ] **Step 3: 提交**

```bash
cd /Users/xiaofang/Documents/www/docker/spider-proxy/code
git add pubspec.yaml
git commit -m "chore(v3.2): 添加 archive 依赖用于 WebSocket 压缩"
```

---

### Task 1.2: 实现 WebSocketCompression 工具类

**Files:**
- Create: `code/lib/core/proxy/websocket_compression.dart`
- Test: `code/test/websocket/websocket_compression_test.dart`

- [ ] **Step 1: 创建 WebSocketCompression 工具类**

```dart
// file: code/lib/core/proxy/websocket_compression.dart
import 'package:archive/archive.dart';
import 'dart:convert';

/// WebSocket 消息压缩工具类
///
/// 支持 RFC 7692 permessage-deflate 扩展
class WebSocketCompression {
  /// 压缩数据
  ///
  /// 使用 deflate 算法压缩二进制数据
  static List<int> compress(List<int> data) {
    final deflate = Deflate();
    return deflate.process(data);
  }

  /// 解压缩数据
  ///
  /// 使用 inflate 算法解压缩二进制数据
  static List<int> decompress(List<int> data) {
    final inflate = Inflate();
    return inflate.process(data);
  }

  /// 压缩文本
  static List<int> compressText(String text) {
    return compress(utf8.encode(text));
  }

  /// 解压为文本
  static String decompressToText(List<int> data) {
    return utf8.decode(decompress(data));
  }
}
```

- [ ] **Step 2: 编写压缩/解压缩测试**

```dart
// file: code/test/websocket/websocket_compression_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:spider_proxy/core/proxy/websocket_compression.dart';
import 'dart:convert';

void main() {
  group('WebSocketCompression', () {
    test('compress and decompress binary data', () {
      final original = utf8.encode('Hello, WebSocket!');
      final compressed = WebSocketCompression.compress(original);
      final decompressed = WebSocketCompression.decompress(compressed);
      expect(decompressed, original);
    });

    test('compress and decompress text', () {
      final originalText = 'Hello, WebSocket!';
      final compressed = WebSocketCompression.compressText(originalText);
      final decompressedText = WebSocketCompression.decompressToText(compressed);
      expect(decompressedText, originalText);
    });

    test('compressed data is smaller than original', () {
      // 创建足够大的数据以体现压缩效果
      final original = utf8.encode('A' * 1000);
      final compressed = WebSocketCompression.compress(original);
      expect(compressed.length, lessThan(original.length));
    });
  });
}
```

- [ ] **Step 3: 运行测试**

```bash
cd code
flutter test test/websocket/websocket_compression_test.dart
```

预期：所有测试通过

- [ ] **Step 4: 提交**

```bash
cd /Users/xiaofang/Documents/www/docker/spider-proxy/code
git add lib/core/proxy/websocket_compression.dart test/websocket/websocket_compression_test.dart
git commit -m "feat(v3.2): 实现 WebSocketCompression 工具类"
```

---

### Task 1.3: 扩展 WebSocketHandshake 支持扩展检测

**Files:**
- Modify: `code/lib/core/proxy/websocket_proxy.dart`

- [ ] **Step 1: 为 WebSocketHandshake 添加扩展字段**

在 `WebSocketHandshake` 类中添加：

```dart
/// WebSocket 握手信息
class WebSocketHandshake {
  final String id;
  final String connectionId;
  final DateTime timestamp;
  final String url;
  final Map<String, String> requestHeaders;
  final Map<String, String>? responseHeaders;
  final int? statusCode;

  // 新增：扩展支持字段
  final String? secWebSocketExtensions;         // 请求的扩展
  final String? secWebSocketExtensionsResponse; // 响应的扩展

  WebSocketHandshake({
    required this.id,
    required this.connectionId,
    required this.timestamp,
    required this.url,
    this.requestHeaders = const {},
    this.responseHeaders,
    this.statusCode,
    this.secWebSocketExtensions,
    this.secWebSocketExtensionsResponse,
  });
```

- [ ] **Step 2: 添加 shouldEnableCompression 方法**

```dart
/// 判断是否启用压缩
bool shouldEnableCompression() {
  if (secWebSocketExtensionsResponse == null) return false;
  return secWebSocketExtensionsResponse!
      .contains('permessage-deflate');
}
```

- [ ] **Step 3: 更新 toJson 方法**

```dart
Map<String, dynamic> toJson() {
  return {
    'id': id,
    'connectionId': connectionId,
    'timestamp': timestamp.toIso8601String(),
    'url': url,
    'requestHeaders': requestHeaders,
    if (responseHeaders != null) 'responseHeaders': responseHeaders,
    if (statusCode != null) 'statusCode': statusCode,
    if (secWebSocketExtensions != null) 'secWebSocketExtensions': secWebSocketExtensions,
    if (secWebSocketExtensionsResponse != null) 'secWebSocketExtensionsResponse': secWebSocketExtensionsResponse,
  };
}
```

- [ ] **Step 4: 编写测试**

```dart
// 添加到 test/websocket/websocket_handshake_test.dart
test('shouldEnableCompression returns true when permessage-deflate is present', () {
  final handshake = WebSocketHandshake(
    id: 'test',
    connectionId: 'conn1',
    url: 'ws://example.com',
    secWebSocketExtensionsResponse: 'permessage-deflate',
  );
  expect(handshake.shouldEnableCompression(), true);
});

test('shouldEnableCompression returns false when extension is not present', () {
  final handshake = WebSocketHandshake(
    id: 'test',
    connectionId: 'conn1',
    url: 'ws://example.com',
  );
  expect(handshake.shouldEnableCompression(), false);
});
```

- [ ] **Step 5: 提交**

```bash
cd /Users/xiaofang/Documents/www/docker/spider-proxy/code
git add lib/core/proxy/websocket_proxy.dart
git commit -m "feat(v3.2): WebSocketHandshake 支持扩展检测"
```

---

### Task 1.4: 扩展 WebSocketConnection 添加压缩字段

**Files:**
- Modify: `code/lib/core/proxy/websocket_proxy.dart`

- [ ] **Step 1: 为 WebSocketConnection 添加压缩字段**

```dart
/// WebSocket 连接
class WebSocketConnection {
  final String id;
  final String url;
  final Map<String, String> headers;
  final List<String>? protocols;

  final ValueNotifier<WebSocketState> state =
      ValueNotifier(WebSocketState.connecting);

  final DateTime connectedAt;
  final List<WebSocketMessage> messages = [];
  final ValueNotifier<int> messageCount = ValueNotifier(0);

  // 元数据
  String? selectedProtocol;
  String? extensions;
  int? closeCode;
  String? closeReason;

  // 新增：压缩相关字段
  final bool isCompressed;           // 是否启用压缩
  int _originalBytes = 0;            // 原始字节数
  int _compressedBytes = 0;          // 压缩后字节数

  WebSocketConnection({
    required this.id,
    required this.url,
    this.headers = const {},
    this.protocols,
    this.isCompressed = false,
  }) : connectedAt = DateTime.now();
```

- [ ] **Step 2: 添加压缩率计算方法**

```dart
/// 获取压缩率 (0.0-1.0, 越小压缩效果越好)
double get compressionRatio {
  if (_originalBytes == 0) return 0.0;
  return _compressedBytes / _originalBytes;
}

/// 获取节省的字节数
int get bytesSaved => _originalBytes - _compressedBytes;

/// 更新压缩统计
void updateCompressionStats(int originalBytes, int compressedBytes) {
  _originalBytes += originalBytes;
  _compressedBytes += compressedBytes;
}
```

- [ ] **Step 3: 更新 markAsConnected 方法**

```dart
/// 标记为已连接
void markAsConnected({String? protocol, String? extensions, bool? compressed}) {
  selectedProtocol = protocol;
  this.extensions = extensions;
  isCompressed = compressed ?? (extensions?.contains('permessage-deflate') ?? false);
  state.value = WebSocketState.open;
}
```

- [ ] **Step 4: 更新 toJson 方法**

```dart
Map<String, dynamic> toJson() {
  return {
    'id': id,
    'url': url,
    'state': state.value.name,
    'connectedAt': connectedAt.toIso8601String(),
    'messageCount': messages.length,
    if (selectedProtocol != null) 'selectedProtocol': selectedProtocol,
    if (extensions != null) 'extensions': extensions,
    if (closeCode != null) 'closeCode': closeCode,
    if (closeReason != null) 'closeReason': closeReason,
    'isCompressed': isCompressed,
    if (_originalBytes > 0) 'originalBytes': _originalBytes,
    if (_compressedBytes > 0) 'compressedBytes': _compressedBytes,
    if (_originalBytes > 0) 'compressionRatio': compressionRatio,
  };
}
```

- [ ] **Step 5: 提交**

```bash
cd /Users/xiaofang/Documents/www/docker/spider-proxy/code
git add lib/core/proxy/websocket_proxy.dart
git commit -m "feat(v3.2): WebSocketConnection 添加压缩统计字段"
```

---

### Task 1.5: 阶段 1 集成测试

**Files:**
- Create: `code/test/websocket/websocket_integration_test.dart`

- [ ] **Step 1: 创建集成测试文件**

```dart
// file: code/test/websocket/websocket_integration_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:spider_proxy/core/proxy/websocket_proxy.dart';
import 'package:spider_proxy/core/proxy/websocket_compression.dart';

void main() {
  group('WebSocket Compression Integration', () {
    test('full compression workflow', () {
      // 创建握手并检测扩展
      final handshake = WebSocketHandshake(
        id: 'hs1',
        connectionId: 'conn1',
        url: 'ws://example.com',
        secWebSocketExtensionsResponse: 'permessage-deflate',
      );

      // 创建连接
      final connection = WebSocketConnection(
        id: 'conn1',
        url: 'ws://example.com',
        isCompressed: handshake.shouldEnableCompression(),
      );

      expect(connection.isCompressed, true);

      // 模拟消息压缩
      final originalMessage = 'Hello, WebSocket! This is a test message.';
      final originalBytes = originalMessage.codeUnits.length;

      final compressed = WebSocketCompression.compressText(originalMessage);
      final compressedBytes = compressed.length;

      // 更新统计
      connection.updateCompressionStats(originalBytes, compressedBytes);

      // 验证压缩率
      expect(connection.compressionRatio, greaterThan(0.0));
      expect(connection.compressionRatio, lessThan(1.0));
      expect(connection.bytesSaved, greaterThan(0));
    });
  });
}
```

- [ ] **Step 2: 运行集成测试**

```bash
cd code
flutter test test/websocket/websocket_integration_test.dart
```

预期：测试通过

- [ ] **Step 3: 阶段 1 完成提交**

```bash
cd /Users/xiaofang/Documents/www/docker/spider-proxy/code
git add test/websocket/websocket_integration_test.dart
git commit -m "test(v3.2): WebSocket 压缩功能集成测试"
```

---

## 阶段 2：WebSocket 帧详情显示

### Task 2.1: 创建 WebSocketFrame 数据结构

**Files:**
- Modify: `code/lib/core/proxy/websocket_proxy.dart`

- [ ] **Step 1: 定义 WebSocketFrame 类**

在 `websocket_proxy.dart` 中添加：

```dart
/// WebSocket 帧
///
/// 表示 RFC 6455 定义的 WebSocket 帧结构
class WebSocketFrame {
  final bool fin;           // FIN bit - 是否为最后一片帧
  final int rsv;            // RSV1-3 (3 bits)
  final int rsv1;           // RSV1 - 压缩时为 1
  final int rsv2;           // RSV2
  final int rsv3;           // RSV3
  final int opcode;         // Opcode (4 bits)
  final bool masked;        // MASK bit
  final int payloadLength;  // Payload length
  final int? extendedPayloadLength; // 扩展长度 (16/64 bit)
  final List<int>? maskingKey; // 掩码 key (如果 MASK=1)

  WebSocketFrame({
    required this.fin,
    this.rsv = 0,
    this.rsv1 = 0,
    this.rsv2 = 0,
    this.rsv3 = 0,
    required this.opcode,
    required this.masked,
    required this.payloadLength,
    this.extendedPayloadLength,
    this.maskingKey,
  });

  /// 获取 Opcode 文本
  String get opcodeText {
    switch (opcode) {
      case 0x0: return 'Continuation';
      case 0x1: return 'Text';
      case 0x2: return 'Binary';
      case 0x8: return 'Close';
      case 0x9: return 'Ping';
      case 0xA: return 'Pong';
      default: return 'Unknown (0x${opcode.toRadixString(16)})';
    }
  }

  /// 获取实际 payload 长度
  int get actualPayloadLength {
    if (payloadLength < 126) {
      return payloadLength;
    } else if (payloadLength == 126) {
      return extendedPayloadLength ?? 0;
    } else {
      // 64-bit length
      return extendedPayloadLength ?? 0;
    }
  }

  /// 是否压缩 (RSV1=1)
  bool get isCompressed => rsv1 == 1;

  /// 获取 RSV 文本表示
  String get rsvText => 'RSV: $rsv (RSV1=$rsv1, RSV2=$rsv2, RSV3=$rsv3)';

  Map<String, dynamic> toJson() {
    return {
      'fin': fin,
      'rsv': rsv,
      'rsv1': rsv1,
      'rsv2': rsv2,
      'rsv3': rsv3,
      'opcode': opcode,
      'opcodeText': opcodeText,
      'masked': masked,
      'payloadLength': payloadLength,
      'actualPayloadLength': actualPayloadLength,
      'isCompressed': isCompressed,
    };
  }

  /// 从二进制数据解析帧头
  static WebSocketFrame? parseHeader(List<int> data) {
    if (data.isEmpty) return null;

    final firstByte = data[0];
    final secondByte = data.length > 1 ? data[1] : 0;

    final fin = (firstByte & 0x80) != 0;
    final rsv1 = (firstByte & 0x40) != 0 ? 1 : 0;
    final rsv2 = (firstByte & 0x20) != 0 ? 1 : 0;
    final rsv3 = (firstByte & 0x10) != 0 ? 1 : 0;
    final rsv = (rsv1 << 2) | (rsv2 << 1) | rsv3;
    final opcode = firstByte & 0x0F;
    final masked = (secondByte & 0x80) != 0;
    int payloadLength = secondByte & 0x7F;

    return WebSocketFrame(
      fin: fin,
      rsv: rsv,
      rsv1: rsv1,
      rsv2: rsv2,
      rsv3: rsv3,
      opcode: opcode,
      masked: masked,
      payloadLength: payloadLength,
    );
  }
}
```

- [ ] **Step 2: 编写 WebSocketFrame 测试**

```dart
// file: code/test/websocket/websocket_frame_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:spider_proxy/core/proxy/websocket_proxy.dart';

void main() {
  group('WebSocketFrame', () {
    test('opcodeText returns correct text', () {
      expect(WebSocketFrame(fin: true, opcode: 0x1, masked: false, payloadLength: 0).opcodeText, 'Text');
      expect(WebSocketFrame(fin: true, opcode: 0x2, masked: false, payloadLength: 0).opcodeText, 'Binary');
      expect(WebSocketFrame(fin: true, opcode: 0x8, masked: false, payloadLength: 0).opcodeText, 'Close');
    });

    test('isCompressed returns true when RSV1=1', () {
      final frame = WebSocketFrame(fin: true, rsv1: 1, opcode: 0x1, masked: false, payloadLength: 100);
      expect(frame.isCompressed, true);
    });

    test('parseHeader extracts correct values', () {
      // 0x81 = FIN=1, RSV=0, Opcode=1 (Text)
      // 0x05 = MASK=0, Length=5
      final data = [0x81, 0x05];
      final frame = WebSocketFrame.parseHeader(data);

      expect(frame?.fin, true);
      expect(frame?.opcode, 0x1);
      expect(frame?.payloadLength, 5);
    });
  });
}
```

- [ ] **Step 3: 运行测试**

```bash
cd code
flutter test test/websocket/websocket_frame_test.dart
```

- [ ] **Step 4: 提交**

```bash
cd /Users/xiaofang/Documents/www/docker/spider-proxy/code
git add lib/core/proxy/websocket_proxy.dart test/websocket/websocket_frame_test.dart
git commit -m "feat(v3.2): 创建 WebSocketFrame 数据结构"
```

---

### Task 2.2: 扩展 WebSocketMessage 支持帧信息

**Files:**
- Modify: `code/lib/core/proxy/websocket_proxy.dart`

- [ ] **Step 1: 为 WebSocketMessage 添加 frame 字段**

```dart
/// WebSocket 消息（增强版）
class WebSocketMessage {
  final String id;
  final String connectionId;
  final DateTime timestamp;
  final MessageDirection direction;
  final MessageType type;
  final dynamic payload;

  // 新增：帧信息
  final WebSocketFrame? frame;

  // 新增：压缩信息
  final bool isCompressed;
  final int? compressedSize;

  WebSocketMessage({
    required this.id,
    required this.connectionId,
    required this.timestamp,
    required this.direction,
    required this.type,
    required this.payload,
    this.frame,
    this.isCompressed = false,
    this.compressedSize,
  });
```

- [ ] **Step 2: 更新 toJson 方法**

```dart
Map<String, dynamic> toJson() {
  return {
    'id': id,
    'connectionId': connectionId,
    'timestamp': timestamp.toIso8601String(),
    'direction': direction.name,
    'type': type.name,
    'size': size,
    if (payload is String) 'payload': payload,
    if (frame != null) 'frame': frame!.toJson(),
    if (isCompressed) 'isCompressed': true,
    if (compressedSize != null) 'compressedSize': compressedSize,
  };
}
```

- [ ] **Step 3: 提交**

```bash
cd /Users/xiaofang/Documents/www/docker/spider-proxy/code
git add lib/core/proxy/websocket_proxy.dart
git commit -m "feat(v3.2): WebSocketMessage 支持帧信息"
```

---

### Task 2.3: 创建 UI 帧详情组件

**Files:**
- Create: `code/lib/ui/widgets/websocket_frame_details.dart`

- [ ] **Step 1: 创建帧详情 UI 组件**

```dart
// file: code/lib/ui/widgets/websocket_frame_details.dart
import 'package:flutter/material.dart';
import 'package:spider_proxy/core/proxy/websocket_proxy.dart';

/// WebSocket 帧详情组件
///
/// 提供分级视图：默认显示基本信息，展开显示详细帧结构
class WebSocketFrameDetails extends StatefulWidget {
  final WebSocketFrame frame;

  const WebSocketFrameDetails({Key? key, required this.frame}) : super(key: key);

  @override
  State<WebSocketFrameDetails> createState() => _WebSocketFrameDetailsState();
}

class _WebSocketFrameDetailsState extends State<WebSocketFrameDetails> {
  bool _isExpanded = false;

  @override
  Widget build(BuildContext context) {
    return Card(
      margin: const EdgeInsets.all(8),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          // 点击展开/收起
          InkWell(
            onTap: () => setState(() => _isExpanded = !_isExpanded),
            child: Padding(
              padding: const EdgeInsets.all(12),
              child: Row(
                children: [
                  Icon(_isExpanded ? Icons.expand_less : Icons.expand_more),
                  const SizedBox(width: 8),
                  const Text('帧详情', style: TextStyle(fontWeight: FontWeight.bold)),
                ],
              ),
            ),
          ),

          // 基本信息（始终显示）
          Padding(
            padding: const EdgeInsets.symmetric(horizontal: 12),
            child: Column(
              crossAxisAlignment: CrossAxisAlignment.start,
              children: [
                _buildInfoRow('类型', widget.frame.opcodeText),
                _buildInfoRow('大小', '${widget.frame.actualPayloadLength} B'),
                _buildInfoRow(
                  '压缩',
                  widget.frame.isCompressed ? '是 ✓' : '否',
                  valueColor: widget.frame.isCompressed ? Colors.green : null,
                ),
              ],
            ),
          ),

          // 详细信息（展开时显示）
          if (_isExpanded) ...[
            const Divider(),
            Padding(
              padding: const EdgeInsets.all(12),
              child: Column(
                crossAxisAlignment: CrossAxisAlignment.start,
                children: [
                  _buildDetailRow('FIN', widget.frame.fin ? '1 (最后一片)' : '0'),
                  _buildDetailRow('RSV', widget.frame.rsvText),
                  _buildDetailRow('Opcode', '0x${widget.frame.opcode.toRadixString(16)} (${widget.frame.opcodeText})'),
                  _buildDetailRow('MASK', widget.frame.masked ? '1' : '0'),
                  _buildDetailRow('Payload Length', '${widget.frame.payloadLength} (${widget.frame.actualPayloadLength} bytes)'),
                  if (widget.frame.maskingKey != null)
                    _buildDetailRow('Masking Key', widget.frame.maskingKey!.join('.')),
                ],
              ),
            ),
          ],
        ],
      ),
    );
  }

  Widget _buildInfoRow(String label, String value, {Color? valueColor}) {
    return Padding(
      padding: const EdgeInsets.symmetric(vertical: 4),
      child: Row(
        mainAxisAlignment: MainAxisAlignment.spaceBetween,
        children: [
          Text(label, style: const TextStyle(color: Colors.grey)),
          Text(value, style: TextStyle(color: valueColor, fontWeight: FontWeight.w500)),
        ],
      ),
    );
  }

  Widget _buildDetailRow(String label, String value) {
    return Padding(
      padding: const EdgeInsets.symmetric(vertical: 4),
      child: Row(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          SizedBox(
            width: 100,
            child: Text(label, style: const TextStyle(fontWeight: FontWeight.bold)),
          ),
          Expanded(
            child: Text(value, style: const TextStyle(fontFamily: 'monospace')),
          ),
        ],
      ),
    );
  }
}
```

- [ ] **Step 2: 提交**

```bash
cd /Users/xiaofang/Documents/www/docker/spider-proxy/code
git add lib/ui/widgets/websocket_frame_details.dart
git commit -m "feat(v3.2): 创建 WebSocketFrameDetails UI 组件"
```

---

### Task 2.4: 更新 WebSocketPage 集成帧详情

**Files:**
- Modify: `code/lib/ui/pages/websocket_page.dart`

- [ ] **Step 1: 读取 websocket_page.dart 了解当前结构**

- [ ] **Step 2: 在消息详情中集成帧详情组件**

（具体实现取决于现有 UI 结构）

- [ ] **Step 3: 提交**

```bash
cd /Users/xiaofang/Documents/www/docker/spider-proxy/code
git add lib/ui/pages/websocket_page.dart
git commit -m "feat(v3.2): WebSocketPage 集成帧详情组件"
```

---

### Task 2.5: 阶段 2 完成提交

- [ ] **Step 1: 运行所有相关测试**

```bash
cd code
flutter test test/websocket/
```

- [ ] **Step 2: 阶段 2 完成提交**

```bash
cd /Users/xiaofang/Documents/www/docker/spider-proxy/code
git commit -m "chore(v3.2): 阶段 2 WebSocket 帧详情显示完成"
```

---

## 阶段 3：脚本调试功能

### Task 3.1: 创建 ScriptLogLevel 和 ScriptExecutionLog

**Files:**
- Modify: `code/lib/core/script/script_engine_v2.dart`

- [ ] **Step 1: 在 script_engine_v2.dart 中添加日志相关类**

在文件开头添加：

```dart
/// 日志级别
enum ScriptLogLevel {
  debug,
  info,
  warn,
  error,
}

/// 脚本执行日志
class ScriptExecutionLog {
  final String id;
  final String scriptId;
  final String scriptName;
  final DateTime timestamp;
  final ScriptLogLevel level;
  final String message;
  final String? stackTrace;
  final String? contextType; // 'onRequest' | 'onResponse'

  ScriptExecutionLog({
    required this.id,
    required this.scriptId,
    required this.scriptName,
    required this.timestamp,
    required this.level,
    required this.message,
    this.stackTrace,
    this.contextType,
  });

  Map<String, dynamic> toJson() {
    return {
      'id': id,
      'scriptId': scriptId,
      'scriptName': scriptName,
      'timestamp': timestamp.toIso8601String(),
      'level': level.name,
      'message': message,
      if (stackTrace != null) 'stackTrace': stackTrace,
      if (contextType != null) 'contextType': contextType,
    };
  }

  factory ScriptExecutionLog.fromJson(Map<String, dynamic> json) {
    return ScriptExecutionLog(
      id: json['id'] as String,
      scriptId: json['scriptId'] as String,
      scriptName: json['scriptName'] as String,
      timestamp: DateTime.parse(json['timestamp'] as String),
      level: ScriptLogLevel.values.firstWhere(
        (e) => e.name == json['level'],
      ),
      message: json['message'] as String,
      stackTrace: json['stackTrace'] as String?,
      contextType: json['contextType'] as String?,
    );
  }
}
```

- [ ] **Step 2: 提交**

```bash
cd /Users/xiaofang/Documents/www/docker/spider-proxy/code
git add lib/core/script/script_engine_v2.dart
git commit -m "feat(v3.2): 添加 ScriptExecutionLog 数据结构"
```

---

### Task 3.2: 实现 ScriptLogManager

**Files:**
- Modify: `code/lib/core/script/script_engine_v2.dart`

- [ ] **Step 1: 添加 ScriptLogManager 类**

```dart
/// 脚本执行日志管理器
class ScriptLogManager {
  final List<ScriptExecutionLog> _logs = [];
  final int _maxLogs = 1000; // 最多保留 1000 条日志

  /// 添加日志
  void addLog(ScriptExecutionLog log) {
    _logs.add(log);
    // 超出限制时删除最旧的日志
    if (_logs.length > _maxLogs) {
      _logs.removeAt(0);
    }
  }

  /// 获取指定脚本的日志
  List<ScriptExecutionLog> getLogsForScript(String scriptId) {
    return _logs.where((log) => log.scriptId == scriptId).toList();
  }

  /// 获取所有日志
  List<ScriptExecutionLog> getAllLogs() {
    return List.unmodifiable(_logs);
  }

  /// 清空日志
  void clearLogs([String? scriptId]) {
    if (scriptId == null) {
      _logs.clear();
    } else {
      _logs.removeWhere((log) => log.scriptId == scriptId);
    }
  }

  /// 获取错误日志数量
  int get errorCount {
    return _logs.where((log) => log.level == ScriptLogLevel.error).length;
  }

  /// 获取日志总数
  int get totalLogs => _logs.length;
}
```

- [ ] **Step 2: 编写测试**

```dart
// file: code/test/script/script_log_manager_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:spider_proxy/core/script/script_engine_v2.dart';

void main() {
  group('ScriptLogManager', () {
    late ScriptLogManager logManager;

    setUp(() {
      logManager = ScriptLogManager();
    });

    test('add and retrieve logs', () {
      final log = ScriptExecutionLog(
        id: '1',
        scriptId: 'script1',
        scriptName: 'Test Script',
        timestamp: DateTime.now(),
        level: ScriptLogLevel.info,
        message: 'Test log',
      );

      logManager.addLog(log);

      expect(logManager.totalLogs, 1);
      expect(logManager.getLogsForScript('script1').length, 1);
    });

    test('clear logs for specific script', () {
      logManager.addLog(ScriptExecutionLog(
        id: '1', scriptId: 'script1', scriptName: 'Script 1',
        timestamp: DateTime.now(), level: ScriptLogLevel.info, message: 'Log 1',
      ));
      logManager.addLog(ScriptExecutionLog(
        id: '2', scriptId: 'script2', scriptName: 'Script 2',
        timestamp: DateTime.now(), level: ScriptLogLevel.info, message: 'Log 2',
      ));

      logManager.clearLogs('script1');

      expect(logManager.totalLogs, 1);
      expect(logManager.getLogsForScript('script1').isEmpty, true);
    });
  });
}
```

- [ ] **Step 3: 运行测试**

```bash
cd code
flutter test test/script/script_log_manager_test.dart
```

- [ ] **Step 4: 提交**

```bash
cd /Users/xiaofang/Documents/www/docker/spider-proxy/code
git add lib/core/script/script_engine_v2.dart test/script/script_log_manager_test.dart
git commit -m "feat(v3.2): 实现 ScriptLogManager 日志管理器"
```

---

### Task 3.3: ScriptEngineV2 集成日志功能

**Files:**
- Modify: `code/lib/core/script/script_engine_v2.dart`

- [ ] **Step 1: 为 ScriptEngineV2 添加日志管理器实例**

```dart
class ScriptEngineV2 {
  EvalDart? _vm;
  bool _enabled = true;
  final List<ScriptV2> _scripts = [];

  // 新增：日志管理器
  final ScriptLogManager _logManager = ScriptLogManager();

  /// 获取日志管理器
  ScriptLogManager get logManager => _logManager;
```

- [ ] **Step 2: 添加日志记录辅助方法**

```dart
// 当前执行上下文
String? _currentScriptId;
String? _currentScriptName;
String? _currentContextType;

String _generateLogId() {
  return DateTime.now().millisecondsSinceEpoch.toString();
}

void _logInfo(String scriptId, String scriptName, String message, String contextType) {
  _logManager.addLog(ScriptExecutionLog(
    id: _generateLogId(),
    scriptId: scriptId,
    scriptName: scriptName,
    timestamp: DateTime.now(),
    level: ScriptLogLevel.info,
    message: message,
    contextType: contextType,
  ));
}

void _logError(String scriptId, String scriptName, String message, String stackTrace, String contextType) {
  _logManager.addLog(ScriptExecutionLog(
    id: _generateLogId(),
    scriptId: scriptId,
    scriptName: scriptName,
    timestamp: DateTime.now(),
    level: ScriptLogLevel.error,
    message: message,
    stackTrace: stackTrace,
    contextType: contextType,
  ));
}
```

- [ ] **Step 3: 修改 _registerLibraries 集成 print 日志**

```dart
_vm!.registerLibrary('proxy://utils', {
  'print': Func1((msg) {
    final log = ScriptExecutionLog(
      id: _generateLogId(),
      scriptId: _currentScriptId ?? 'unknown',
      scriptName: _currentScriptName ?? 'unknown',
      timestamp: DateTime.now(),
      level: ScriptLogLevel.info,
      message: msg as String,
      contextType: _currentContextType,
    );
    _logManager.addLog(log);
    print('[Script] $msg');
    return null;
  }),
  'jsonDecode': Func1((str) => json.decode(str as String)),
  'jsonEncode': Func1((obj) => json.encode(obj)),
});
```

- [ ] **Step 4: 修改 executeOnRequest 添加错误日志**

```dart
Future<ScriptContext?> executeOnRequest(
  String url,
  String method,
  Map<String, String> headers,
  String body,
) async {
  if (!_enabled) return null;
  if (_vm == null) await initialize();

  for (final script in _scripts) {
    if (!script.enabled) continue;
    if (script.type != ScriptType.onRequest &&
        script.type != ScriptType.universal) continue;

    _currentScriptId = script.id;
    _currentScriptName = script.name;
    _currentContextType = 'onRequest';

    try {
      final context = ScriptContext(
        url: url,
        method: method,
        headers: Map.from(headers),
        body: body,
      );

      final result = await _executeScript(script.code, context, 'onRequest');
      if (result != null && result.modified) {
        _logInfo(script.id, script.name, 'Request modified successfully', 'onRequest');
        return result;
      }
    } catch (e, stackTrace) {
      _logError(script.id, script.name, 'Error: ${e.toString()}', stackTrace.toString(), 'onRequest');
    }
  }

  return null;
}
```

- [ ] **Step 5: 修改 executeOnResponse 类似处理**

- [ ] **Step 6: 提交**

```bash
cd /Users/xiaofang/Documents/www/docker/spider-proxy/code
git add lib/core/script/script_engine_v2.dart
git commit -m "feat(v3.2): ScriptEngineV2 集成日志记录"
```

---

### Task 3.4: 创建 UI 日志视图组件

**Files:**
- Create: `code/lib/ui/widgets/script_log_view.dart`

- [ ] **Step 1: 创建日志视图组件**

```dart
// file: code/lib/ui/widgets/script_log_view.dart
import 'package:flutter/material.dart';
import 'package:spider_proxy/core/script/script_engine_v2.dart';

/// 脚本日志视图组件
class ScriptLogView extends StatefulWidget {
  final ScriptLogManager logManager;
  final String scriptId;
  final String scriptName;

  const ScriptLogView({
    Key? key,
    required this.logManager,
    required this.scriptId,
    required this.scriptName,
  }) : super(key: key);

  @override
  State<ScriptLogView> createState() => _ScriptLogViewState();
}

class _ScriptLogViewState extends State<ScriptLogView> {
  ScriptLogLevel? _filterLevel;

  List<ScriptExecutionLog> _getFilteredLogs() {
    final logs = widget.logManager.getLogsForScript(widget.scriptId);
    if (_filterLevel == null) return logs;
    return logs.where((log) => log.level == _filterLevel).toList();
  }

  Color _getLevelColor(ScriptLogLevel level) {
    switch (level) {
      case ScriptLogLevel.debug: return Colors.grey;
      case ScriptLogLevel.info: return Colors.blue;
      case ScriptLogLevel.warn: return Colors.orange;
      case ScriptLogLevel.error: return Colors.red;
    }
  }

  @override
  Widget build(BuildContext context) {
    final logs = _getFilteredLogs();

    return Column(
      children: [
        // 工具栏
        Padding(
          padding: const EdgeInsets.all(8),
          child: Row(
            children: [
              Text('脚本日志：${widget.scriptName}',
                  style: const TextStyle(fontWeight: FontWeight.bold)),
              const Spacer(),
              // 过滤按钮
              _buildFilterButton('全部', null),
              _buildFilterButton('INFO', ScriptLogLevel.info),
              _buildFilterButton('ERROR', ScriptLogLevel.error),
              const Spacer(),
              // 清空按钮
              IconButton(
                icon: const Icon(Icons.delete_outline),
                tooltip: '清空日志',
                onPressed: () {
                  widget.logManager.clearLogs(widget.scriptId);
                  setState(() {});
                },
              ),
            ],
          ),
        ),

        const Divider(height: 1),

        // 日志列表
        Expanded(
          child: logs.isEmpty
              ? const Center(child: Text('暂无日志'))
              : ListView.builder(
                  itemCount: logs.length,
                  itemBuilder: (context, index) {
                    final log = logs[index];
                    return _buildLogTile(log);
                  },
                ),
        ),
      ],
    );
  }

  Widget _buildFilterButton(String label, ScriptLogLevel? level) {
    final isSelected = _filterLevel == level;
    return Padding(
      padding: const EdgeInsets.symmetric(horizontal: 4),
      child: FilterChip(
        label: Text(label),
        selected: isSelected,
        onSelected: (selected) {
          setState(() {
            _filterLevel = selected ? level : null;
          });
        },
      ),
    );
  }

  Widget _buildLogTile(ScriptExecutionLog log) {
    return ListTile(
      dense: true,
      leading: Container(
        width: 8,
        height: 8,
        decoration: BoxDecoration(
          color: _getLevelColor(log.level),
          shape: BoxShape.circle,
        ),
      ),
      title: Text(
        log.message,
        style: const TextStyle(fontSize: 12),
      ),
      subtitle: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          const SizedBox(height: 4),
          Text(
            '${_formatTime(log.timestamp)} • ${log.contextType ?? "unknown"}',
            style: const TextStyle(fontSize: 10, color: Colors.grey),
          ),
          if (log.stackTrace != null) ...[
            const SizedBox(height: 4),
            Container(
              padding: const EdgeInsets.all(8),
              decoration: BoxDecoration(
                color: Colors.red.shade50,
                borderRadius: BorderRadius.circular(4),
              ),
              child: Text(
                log.stackTrace!,
                style: const TextStyle(fontSize: 10, fontFamily: 'monospace', color: Colors.red),
              ),
            ),
          ],
        ],
      ),
    );
  }

  String _formatTime(DateTime time) {
    return '${time.hour.toString().padLeft(2, '0')}:${time.minute.toString().padLeft(2, '0')}:${time.second.toString().padLeft(2, '0')}.${time.millisecond.toString().padLeft(3, '0')}';
  }
}
```

- [ ] **Step 2: 提交**

```bash
cd /Users/xiaofang/Documents/www/docker/spider-proxy/code
git add lib/ui/widgets/script_log_view.dart
git commit -m "feat(v3.2): 创建 ScriptLogView UI 组件"
```

---

### Task 3.5: 更新 ScriptManagerPage 集成日志视图

**Files:**
- Modify: `code/lib/ui/pages/script_manager_page.dart`

- [ ] **Step 1: 读取 script_manager_page.dart 了解当前结构**

- [ ] **Step 2: 在脚本列表项中添加"日志"按钮**

- [ ] **Step 3: 点击日志按钮时打开日志视图对话框**

- [ ] **Step 4: 提交**

```bash
cd /Users/xiaofang/Documents/www/docker/spider-proxy/code
git add lib/ui/pages/script_manager_page.dart
git commit -m "feat(v3.2): ScriptManagerPage 集成日志视图"
```

---

### Task 3.6: 阶段 3 完成提交

- [ ] **Step 1: 运行所有相关测试**

```bash
cd code
flutter test test/script/
```

- [ ] **Step 2: 阶段 3 完成提交**

```bash
cd /Users/xiaofang/Documents/www/docker/spider-proxy/code
git commit -m "chore(v3.2): 阶段 3 脚本调试功能完成"
```

---

## v3.2 完成

### Task 4.1: 最终集成测试

- [ ] **Step 1: 运行全部测试**

```bash
cd code
flutter test
```

- [ ] **Step 2: 验证所有功能正常工作**

### Task 4.2: 更新文档

- [ ] **Step 1: 更新 v3.2 发布说明**

### Task 4.3: 提交最终版本

```bash
cd /Users/xiaofang/Documents/www/docker/spider-proxy
git add code
git commit -m "chore: 更新 code 子模组至 v3.2 完成状态"
```
